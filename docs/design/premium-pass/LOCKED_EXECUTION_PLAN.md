# Nexova / 4Geeks 4g-006 — Locked Execution Plan

**Status:** EXECUTION LOCK / VISUAL V2 RECONCILED  
**Date:** 2026-09-24  
**Current cursor:** S1 Website Premium Pass → R0 Visual Canon Recovery  
**Current-state authority:** `CURRENT_EXECUTION_STATE.yaml` + `PROJECT_STATE.md`

## Authority order

1. 4Geeks rubric / assignment contract
2. Product Truth
3. Identity Base Lock
4. Direction + Asset contracts
5. current workstream plan
6. canonical visual artifact once approved
7. repository architecture
8. runtime evidence

Historical chat and historical approval snapshots are not current operational state.

## Macro lock

```text
S0 ACADEMIC_BASELINE_PASS          ✅
 ↓
S1 WEBSITE_PREMIUM_PASS           ← CURRENT
    R0 Visual Canon Recovery      ← CURRENT
    R1 Five Composition Frames
    H1.5 Human Visual Gate
    R2 v0 Runtime Prototype
    H1.6 Human Runtime Gate
    R3 Figma Golden V2
    H2 V2 Human Gate
    R4 Codex Production
    R5 Visual + Technical Red Team
    R6 H3 Runtime Human Gate
 ↓
S2 BACKOFFICE_PREMIUM_PASS
 ↓
S3 NEXOVA_FOUNDATION_PASS
 ↓
S4 INTEGRATED_QA_PASS
 ↓
S5 RUBRIC_REFRESH_PASS
 ↓
S6 FINAL_PR_READY
 ↓
S7 USER_APPROVED
 ↓
S8 SUBMITTED
 ↓
S9 DIDACTIC_PASS
 ↓
S10 HARVEST_COMPLETE
```

## Visual adjudication

The previous H2 approval is retained as historical evidence but is **reopened as current visual authority** after material human H3 rejection.

Reason:
- technically correct implementation remained aesthetically below target;
- H2 V1 did not sufficiently encode production-grade art direction;
- critical visual artifacts were represented by low-resolution Figma proxies;
- the runtime exposed excessive design freedom left to implementation.

This does not reopen the identity, product truth, architecture, typography family decision, A1/A2 semantic roles, or Hero→Manifesto narrative.

Current visual plan:
`docs/design/premium-pass/VISUAL_RECOVERY_PLAN_V2.md`.

## v0 adjudication

Historical v0 challenger remains closed.

A **new bounded v0 runtime-design pass is authorized only after H1.5** because the original cost-gate trigger is now met:
1. material visual problem survived Codex;
2. Golden/contracts alone did not resolve it;
3. runtime visual validation now has clear expected value.

Budget:
- one main generation;
- maximum one bounded repair.

v0 does not become visual authority and cannot be merged wholesale.

## Production ownership

- Figma H2 V2 → canonical visual authority after human approval.
- v0 → runtime design prototype only.
- Codex → production implementation only.
- GSAP → only the single approved Hero→Manifesto narrative.
- CI → technical verification, not aesthetic approval.
- Human gates → visual acceptance.

## Academic lock

- academic baseline remains recoverable;
- Control Tower baseline: 22/22 blocking PASS;
- required final branch remains `feature/agent-memory-bank`;
- update existing PR #1 only;
- final R01–R22 refresh is S5, not S1;
- Campus submission remains user-only.

## Noema boundary

The forthcoming transversal Noema version may coordinate this plan but may not overwrite project state or visual authority. Read the root checkpoint first.

## Stop rules

Do not:
- continue patching the current runtime as if it were visually canonical;
- create new identity routes;
- call v0 before R1 human approval;
- start S2 before Website Premium PASS;
- convert technical green checks into visual PASS.

Progress is measured in visible artifacts:
`canonical asset → frames → runtime prototype → Golden → production runtime`.
