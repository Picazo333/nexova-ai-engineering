# Nexova Domain Sources

This directory tracks company/domain source authority used by implementations.

`SOURCE_MANIFEST.yaml` classifies material as primary, current, derived, historical, candidate or conflicted. Domain-specific source copies should be added here only when provenance is known and retaining them in Git is appropriate.

Do not copy historical strategy into current product requirements merely to make the context richer.
